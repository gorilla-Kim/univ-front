import React from 'react';
import styled from "styled-components";

const Home = (props) => {

    return (
        <Wrapper>
           Home
        </Wrapper>
    )
};

const Wrapper = styled.div`
`;

export default Home;