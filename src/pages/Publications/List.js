import React from 'react';
import styled from "styled-components";

const List = (props) => {

    return (
        <Wrapper>
           Publication List
        </Wrapper>
    )
};

const Wrapper = styled.div`
`;

export default List;