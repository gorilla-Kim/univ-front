import React from "react";
import styled from "styled-components";

const PostDetail = (props) => {
  return <Wrapper>PostDetail</Wrapper>;
};

const Wrapper = styled.div``;

export default PostDetail;
